
generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
}

model user { 
  id    Int     @id @default(autoincrement()) 
  email String  @unique
  name  String
  password String
} 
